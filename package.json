{
  "name": "@b10cks/sdk",
  "version": "2.3.1",
  "description": "Official b10cks SDKs",
  "license": "MIT",
  "author": {
    "name": "Michael Wallner",
    "email": "m.wallner@coderscantina.com",
    "url": "https://github.com/b10cks"
  },
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "build": "pnpm run build:packages",
    "build:packages": "pnpm --filter='@b10cks/client' run build && pnpm --filter='@b10cks/mgmt-client' run build && pnpm --filter='@b10cks/mcp-server' run build && pnpm --filter='@b10cks/cli' run build && pnpm --filter='@b10cks/richtext' run build && pnpm --filter='@b10cks/vue' run build && pnpm --filter='@b10cks/react' run build && pnpm --filter='@b10cks/svelte' run build && pnpm --filter='@b10cks/nuxt' run build && pnpm --filter='@b10cks/next' run build",
    "test": "pnpm --filter='*' run test",
    "typecheck": "pnpm --filter='*' --if-present run typecheck",
    "lint": "oxlint --deny-warnings .",
    "lint:fix": "oxlint --fix .",
    "format": "oxfmt --write .",
    "clean": "pnpm --filter='*' run clean",
    "version:check": "pnpm run scripts/version-check.js",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "changeset:publish": "changeset publish",
    "release": "pnpm run build && pnpm run changeset:publish"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.31.1",
    "@vitejs/plugin-vue": "^6.0.8",
    "ajv": "^8.20.0",
    "oxfmt": "^0.60.0",
    "oxlint": "^1.75.0",
    "typescript": "^5.9.3",
    "vite": "^8.1.5",
    "vite-plugin-dts": "^5.0.3"
  },
  "engines": {
    "node": ">=24.7.0"
  },
  "packageManager": "pnpm@10.15.1",
  "private": true
}
