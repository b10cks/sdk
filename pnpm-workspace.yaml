packages:
  - 'packages/*'

overrides:
  next: '>=16.2.6'

# Dependencies allowed to run install scripts. pnpm 11 fails the install instead
# of warning when a package with a build script is not listed here.
allowBuilds:
  '@parcel/watcher': true
  esbuild: true
  sharp: true
